version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
# cache:    <-- delete this whole section for one clean build
#   paths:
#     - node_modules/**/*
